{
  "name": "password-health-root",
  "private": true,
  "version": "0.1.0",
  "description": "Root package for Vercel framework detection in monorepo layout.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "dependencies": {
    "next": "15.0.0"
  }
}
